# Agent workspaces

Each folder is a clean starting workspace with an example instruction file for
that agent. Replace the example with project-specific rules before doing real
work. Keep repositories below `/home/fremen/projects` or inside the corresponding
agent folder; both locations persist in the `arrakis-usul` Docker volume.
